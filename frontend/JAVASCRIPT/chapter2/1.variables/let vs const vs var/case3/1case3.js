//3rd diffrence 
// "let","const" can be local , global as well as  block variable, in the realm of var, it can be only local and global if it is insdie the block then it is treated as outside the block

// so if outside the block there is function then it is treatd as local and if outsite is not any function then it is treated as global
