//what is path parameter:

//A path parameter is a dynamic route  that gets replaced by the value sent in the request URL

//practical use of path paramter 

//The practical use of path parameters is when returning a specific user’s profile. Instead of creating separate endpoints for each user, we use one dynamic endpoint like /profile/:userid, and the :userid gets replaced by the user’s actual ID.